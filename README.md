# mock-ai-provider

Provider-compatible mock servers for OpenAI, Anthropic, and other AI APIs.

This package is standalone infrastructure. Clients call it through normal
provider-compatible HTTP APIs.
